import Foundation

/// Shared test utilities and helpers for the TaskManager test suite.
///
/// This module provides common test infrastructure such as mock objects,
/// fixture factories, and assertion helpers used across multiple test targets.
public enum TestSupport {
    /// A placeholder to ensure the module compiles.
    /// Additional test utilities will be added as needed.
    public static let version = "1.0.0"
}
